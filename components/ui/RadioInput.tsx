import React from 'react';
import { View } from 'react-native';

type RadioInputProps = {
	items: any[],
	label: string,
	value: string,
	defaultValue: string,
	onChange: (value: string) => void
}

export default function RadioInput({}: RadioInputProps) {
  return (
    <View>
		</View>
  );
}

